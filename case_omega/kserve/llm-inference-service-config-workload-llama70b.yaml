apiVersion: serving.kserve.io/v1alpha1
kind: LLMInferenceServiceConfig
metadata:
  name: llm-inference-service-config-workload-llama70b
  namespace: omega
spec:
  replicas: 1
  template:
    nodeSelector:
      node-role.kubernetes.io/gpu-node: "true"
      gpu-type: "rtx-pro-5000-blackwell"
    tolerations:
      - key: "gpu-node"
        operator: "Equal"
        value: "true"
        effect: "NoSchedule"
    imagePullSecrets:
      - name: registry-credentials
    containers:
      - name: main
        image: vllm/vllm-openai:latest
        command:
          - python3
          - -m
          - vllm.entrypoints.openai.api_server
        args:
          - --model
          - hugging-quants/Meta-Llama-3.1-70B-Instruct-AWQ-INT4
          - --port
          - "8000"
          - --host
          - "0.0.0.0"
          - --dtype
          - auto
          - --quantization
          - awq
          - --tensor-parallel-size
          - "1"
          - --max-model-len
          - "16384"
          - --max-num-seqs
          - "4"
          - --gpu-memory-utilization
          - "0.90"
          - --download-dir
          - /mnt/huggingface/models
          - --enable-prefix-caching
          - --enable-auto-tool-choice
          - --tool-call-parser
          - hermes
        env:
          - name: HF_TOKEN
            valueFrom:
              secretKeyRef:
                name: hf-token
                key: token
          - name: PYTHONUNBUFFERED
            value: "1"
        volumeMounts:
          - name: dshm
            mountPath: /dev/shm
          - name: model-cache
            mountPath: /mnt/huggingface/models
        resources:
          limits:
            nvidia.com/gpu: "1"
            memory: 44Gi
          requests:
            nvidia.com/gpu: "1"
            memory: 40Gi
        securityContext:
          runAsNonRoot: false
          capabilities:
            drop:
              - ALL
    volumes:
      - name: dshm
        emptyDir:
          medium: Memory
          sizeLimit: 8Gi
      - name: model-cache
        emptyDir: {}
