apiVersion: v1
kind: Service
metadata:
  name: nextchat
  namespace: frontend
spec:
  type: ClusterIP
  selector:
    app: nextchat
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
