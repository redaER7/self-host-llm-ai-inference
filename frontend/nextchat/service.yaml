apiVersion: v1
kind: Service
metadata:
  name: nextchat
  namespace: frontend
spec:
  type: NodePort
  selector:
    app: nextchat
  ports:
    - name: https
      port: 443
      targetPort: 3000
      nodePort: 30081
      protocol: TCP
