apiVersion: v1
kind: Service
metadata:
  name: nextchat
  namespace: frontend
spec:
  type: NodePort
  selector:
    app: nextchat
  ports:
    - name: http
      port: 80
      targetPort: 3000
      nodePort: 3080
      protocol: TCP
