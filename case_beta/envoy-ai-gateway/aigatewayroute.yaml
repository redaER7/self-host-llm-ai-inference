apiVersion: aigateway.envoyproxy.io/v1beta1
kind: AIGatewayRoute
metadata:
  name: beta-route
  namespace: beta
spec:
  parentRefs:
    - name: ai-gateway
      namespace: envoy-ai-gateway-system
  rules:
    - matches:
        - headers:
            - name: x-ai-eg-model
              value: "Qwen/Qwen2.5-32B-Instruct-AWQ"
      backendRefs:
        - name: llm-server-backend
      timeouts:
        # Overrides controller default 60s — long non-streaming decode batches run 60-240s
        request: 300s
  llmRequestCosts:
    - metadataKey: llm_input_token
      type: InputToken
    - metadataKey: llm_output_token
      type: OutputToken
    - metadataKey: llm_total_token
      type: TotalToken